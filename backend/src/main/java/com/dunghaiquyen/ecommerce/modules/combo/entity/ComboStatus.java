package com.dunghaiquyen.ecommerce.modules.combo.entity;

/** ACTIVE combos are evaluated at checkout; INACTIVE ones are ignored (soft off-switch). */
public enum ComboStatus {
    ACTIVE,
    INACTIVE
}
