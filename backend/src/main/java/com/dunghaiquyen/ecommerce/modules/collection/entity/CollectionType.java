package com.dunghaiquyen.ecommerce.modules.collection.entity;

/** Exact set the V10 chk_collections_type constraint allows. */
public enum CollectionType {
    SEASONAL,
    SPORT,
    CAMPAIGN,
    CAPSULE,
    NEW_ARRIVAL
}
