package com.dunghaiquyen.ecommerce.modules.returns.entity;

/** Exact set V3's chk_return_items_condition_status check constraint allows. */
public enum ReturnItemConditionStatus {
    UNOPENED,
    LIKE_NEW,
    USED,
    DAMAGED
}
