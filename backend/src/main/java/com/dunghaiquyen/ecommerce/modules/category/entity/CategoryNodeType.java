package com.dunghaiquyen.ecommerce.modules.category.entity;

/**
 * GROUP = nút gom nhóm taxonomy, LEAF = danh mục lá gắn trực tiếp cho product.
 */
public enum CategoryNodeType {
    GROUP,
    LEAF
}
