package com.dunghaiquyen.ecommerce.modules.returns.entity;

/** Exact set V3's chk_returns_status check constraint allows. */
public enum ReturnStatus {
    REQUESTED,
    APPROVED,
    REJECTED,
    RECEIVED,
    REFUNDED,
    CANCELLED
}
