package com.dunghaiquyen.ecommerce.modules.banner.entity;

/** Exact set V3's chk_banners_placement check constraint allows. */
public enum BannerPlacement {
    HOME_HERO,
    HOME_GRID,
    COLLECTION_TOP,
    SIDEBAR,
    POPUP,
    FOOTER
}
